---
phase: 06-task-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/task-store.tsx
  - src/components/TaskCard.tsx
  - src/components/TaskSprite.tsx
  - src/components/TaskDetail.tsx
  - src/components/HorizonScene.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking any task card opens the TaskDetail panel on the right side"
    - "Clicking any task sprite opens the TaskDetail panel on the right side"
    - "Clicking the backdrop or X button closes the panel"
    - "The panel displays the selected task's title, details, horizon, drift count, and action buttons"
    - "Scrolling the canvas still works when hovering over task cards"
  artifacts:
    - path: "src/components/TaskDetail.tsx"
      provides: "Slide-in detail panel overlay"
      min_lines: 80
    - path: "src/stores/task-store.tsx"
      provides: "selectedTaskId state + selectTask/clearSelection actions"
      contains: "selectedTaskId"
  key_links:
    - from: "src/components/TaskSprite.tsx"
      to: "src/stores/task-store.tsx"
      via: "onClick -> store.selectTask(task.id)"
      pattern: "selectTask"
    - from: "src/components/TaskCard.tsx"
      to: "src/stores/task-store.tsx"
      via: "onClick on inner div -> store.selectTask(task.id)"
      pattern: "selectTask"
    - from: "src/components/TaskDetail.tsx"
      to: "src/stores/task-store.tsx"
      via: "useTaskStore(s => s.selectedTaskId) subscription"
      pattern: "selectedTaskId"
---

<objective>
Wire click-to-panel: add selectedTaskId to Zustand store, make TaskCard and TaskSprite clickable, create TaskDetail.tsx as a slide-in DOM overlay panel that displays task info and action stubs.

Purpose: This is the foundation for all Phase 6 interactions -- nothing works until clicking a task opens its detail panel.
Output: Clicking any task in the 3D scene opens a glass-panel overlay on the right with task info. Action buttons (Complete, Drop, Reschedule) are rendered but not yet wired.
</objective>

<execution_context>
@/Users/marcus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-task-interactions/06-CONTEXT.md
@.planning/phases/06-task-interactions/06-RESEARCH.md
@src/stores/task-store.tsx
@src/components/TaskCard.tsx
@src/components/TaskSprite.tsx
@src/components/TaskNode.tsx
@src/components/HorizonScene.tsx
@src/components/InputBubble.tsx
@src/components/SnapToPresent.tsx
@src/lib/horizons.ts
@src/types/task.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store extensions + click handlers on TaskCard and TaskSprite</name>
  <files>src/stores/task-store.tsx, src/components/TaskCard.tsx, src/components/TaskSprite.tsx</files>
  <action>
  **task-store.tsx** -- Add to TaskState:
  - `selectedTaskId: string | null` (initial: null)

  Add to TaskActions:
  - `selectTask: (id: string) => void` -- sets selectedTaskId
  - `clearSelection: () => void` -- sets selectedTaskId to null

  Export `useSelectedTask()` derived selector that returns the full `Task` (with computed horizon) or null. This selector should find the task by selectedTaskId, compute horizon from targetDate fields using `getHorizon()`, and return a `Task` object. This avoids every consumer re-deriving horizon.

  **TaskCard.tsx** -- Make the card clickable:
  - Keep `Html` wrapper with `style={{ pointerEvents: 'none' }}` (CRITICAL: do not change this or scroll breaks when hovering cards)
  - Add `pointerEvents: 'auto'` to the inner `<div style={cardStyle}>` only
  - Add `cursor: 'pointer'` to cardStyle
  - Add `onClick` on the inner div: `() => store?.getState().selectTask(task.id)`

  **TaskSprite.tsx** -- Make the sprite clickable:
  - Add `onClick` to the `<mesh>` element: `(e) => { e.stopPropagation(); store?.getState().selectTask(task.id); }`
  - `e.stopPropagation()` prevents the click from hitting objects behind the sprite
  - Add `cursor: 'pointer'` style to the mesh (R3F supports this via onPointerOver/onPointerOut setting document.body.style.cursor)
  - Actually implement pointer cursor: add `onPointerOver={() => { document.body.style.cursor = 'pointer' }}` and `onPointerOut={() => { document.body.style.cursor = 'auto' }}` to the mesh
  </action>
  <verify>
  - `npm run build` compiles without errors
  - Run the dev server, click a task card -- check that the store's selectedTaskId updates (visible via React DevTools or console log)
  - Scroll while hovering a task card -- scroll still works (pointerEvents: 'none' on Html wrapper preserved)
  </verify>
  <done>Clicking any task card or sprite sets selectedTaskId in the Zustand store. Scroll is not broken on card hover.</done>
</task>

<task type="auto">
  <name>Task 2: TaskDetail slide-in panel + render in HorizonScene</name>
  <files>src/components/TaskDetail.tsx, src/components/HorizonScene.tsx</files>
  <action>
  **TaskDetail.tsx** -- Create as a plain DOM overlay component (NOT inside Canvas). Follow the inline-styles-only pattern used by SnapToPresent, InputBubble, and all drei Html components. No Tailwind.

  Panel structure:
  - Fixed position, right: 0, top: 0, width: 480px, maxWidth: '100vw', height: '100vh'
  - Background: `rgba(10, 10, 15, 0.92)` with `backdropFilter: blur(16px)` + `WebkitBackdropFilter: blur(16px)`
  - Border-left: `1px solid rgba(148, 163, 184, 0.15)`
  - z-index: 120 (above InputBubble at 110, SnapToPresent at 100)
  - Slide-in animation via CSS transform: `translateX(0)` when open, `translateX(100%)` when closed
  - Transition: `transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1)` for spring overshoot feel
  - Color: `#e2e8f0`, fontFamily: `monospace`

  Panel layout (flex column):
  1. **Header row** -- Close button (X) at top-right, task title displayed as an `<input>` element (editable, but save logic wired in Plan 03). For now, just display with `readOnly` attribute. Style: fontSize 18, fontWeight 600, background transparent, border none, color inherit, width 100%.
  2. **Info section** -- Horizon badge (e.g., "This Week" in a small pill), drift count if > 0 ("Drifted 3x" in muted amber), created date.
  3. **Notes/details area** -- A `<textarea>` for task details/notes. For now, display `task.rawInput` as placeholder content, `readOnly`. Style: background `rgba(255,255,255,0.03)`, border `1px solid rgba(148,163,184,0.1)`, borderRadius 8, minHeight 120, padding 12, resize vertical.
  4. **Action bar** (at bottom, `marginTop: 'auto'`) -- Three buttons in a row:
     - "Complete" button (green-tinted, e.g., background `rgba(34, 197, 94, 0.15)`, border `rgba(34, 197, 94, 0.3)`)
     - "Drop" button (muted red, e.g., background `rgba(239, 68, 68, 0.1)`, border `rgba(239, 68, 68, 0.2)`)
     - "Reschedule" button (blue-tinted, e.g., background `rgba(59, 130, 246, 0.1)`, border `rgba(59, 130, 246, 0.2)`)
     - All buttons: borderRadius 8, padding '8px 16px', cursor pointer, fontSize 13, fontFamily monospace
     - Action handlers are stubs for now: `console.log('complete', task.id)` etc.

  Backdrop: Render a semi-transparent backdrop div behind the panel (z-index 119) with `pointerEvents: 'auto'` and `onClick` to call `clearSelection()`. Background: `rgba(0, 0, 0, 0.3)`. Only render when panel is open.

  Use `useSelectedTask()` from task-store to get the task. If null, render nothing (or render the panel off-screen via translateX(100%)).

  IMPORTANT: Use `useTaskStore(s => s.selectedTaskId)` to determine open/closed state, and `useSelectedTask()` for the task data. Render the panel structure always (for CSS transition), toggle via transform.

  Horizon label mapping: Reuse the HORIZON_LABELS pattern from InputBubble (or define locally): immediate -> "Now", this-week -> "This Week", etc.

  **HorizonScene.tsx** -- Add `<TaskDetail />` as a sibling to Canvas, after InputBubble:
  ```
  <Canvas ...>
    <SceneContents />
  </Canvas>
  <SnapToPresent />
  <InputBubble />
  <TaskDetail />
  <DebugOverlay ... />
  ```

  Import TaskDetail at the top of HorizonScene.tsx.
  </action>
  <verify>
  - `npm run build` compiles without errors
  - Run dev server, click a task card -- panel slides in from the right with task title, horizon badge, raw input, and action buttons
  - Click backdrop or X -- panel slides out
  - Click a different task while panel is open -- panel updates to show the new task
  - Panel does not interfere with InputBubble or SnapToPresent button below it
  </verify>
  <done>TaskDetail panel slides in from the right when any task is clicked, displays task info (title, horizon, drift, rawInput), shows Complete/Drop/Reschedule action stubs. Backdrop click and X button close the panel.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero type errors
- Clicking any task card or sprite opens the TaskDetail panel
- The panel shows correct task data (title, horizon, drift count, rawInput)
- Panel slides in/out with spring overshoot animation
- Clicking backdrop or X closes the panel
- Scrolling still works when hovering over task cards (pointerEvents gotcha handled)
- InputBubble and SnapToPresent remain functional while panel is open
</verification>

<success_criteria>
1. selectedTaskId flows from R3F click through Zustand to DOM TaskDetail panel
2. Panel renders as fixed DOM overlay at z-index 120 with glass styling
3. All task info visible: title, horizon badge, drift count, raw input, action buttons
4. Panel open/close transitions with spring cubic-bezier easing
5. No scroll breakage on card hover
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-interactions/06-01-SUMMARY.md`
</output>
