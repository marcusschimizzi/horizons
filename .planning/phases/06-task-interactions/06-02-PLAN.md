---
phase: 06-task-interactions
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/TaskDetail.tsx
  - src/components/CompletionBurst.tsx
  - src/components/HorizonScene.tsx
  - src/components/TaskNode.tsx
  - src/components/TaskSprite.tsx
  - src/components/TaskCard.tsx
  - src/stores/task-store.tsx
  - src/app/api/tasks/route.ts
autonomous: true

must_haves:
  truths:
    - "Completing a task from the detail panel dissolves it with a white/gold particle burst"
    - "After completion, an undo toast appears for ~4 seconds allowing reversal"
    - "Dropping a task removes it immediately with no celebration and no undo"
    - "Completed and dropped tasks no longer appear in the scene after page refresh"
    - "Complete and drop are emotionally distinct animations"
  artifacts:
    - path: "src/components/CompletionBurst.tsx"
      provides: "R3F Points particle burst effect at task position"
      min_lines: 40
    - path: "src/app/api/tasks/route.ts"
      provides: "GET endpoint filtered to status=active only"
      contains: "eq(tasks.status"
  key_links:
    - from: "src/components/TaskDetail.tsx"
      to: "src/stores/task-store.tsx"
      via: "handleComplete calls removeTask + schedules PATCH"
      pattern: "removeTask|handleComplete"
    - from: "src/components/CompletionBurst.tsx"
      to: "three"
      via: "useFrame animates Points particle positions/opacity"
      pattern: "useFrame|pointsRef"
    - from: "src/components/HorizonScene.tsx"
      to: "src/components/CompletionBurst.tsx"
      via: "completionBursts state array renders CompletionBurst components"
      pattern: "CompletionBurst"
    - from: "src/app/api/tasks/route.ts"
      to: "drizzle-orm"
      via: "WHERE status = 'active' filter on GET"
      pattern: "eq.*status.*active"
---

<objective>
Wire the Complete and Drop actions: completing a task plays a white/gold particle burst dissolution animation and shows an undo toast; dropping a task removes it instantly with no ceremony. Both persist to Postgres via PATCH (status change). The GET /api/tasks route is filtered to return only active tasks.

Purpose: The two most important mutation actions -- finishing work (celebratory) and letting go (clinical). The emotional contrast between them is core to the app's feel.
Output: Complete and Drop buttons in TaskDetail are fully functional with animations, persistence, and the GET route only returns active tasks.
</objective>

<execution_context>
@/Users/marcus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-task-interactions/06-CONTEXT.md
@.planning/phases/06-task-interactions/06-RESEARCH.md
@.planning/phases/06-task-interactions/06-01-SUMMARY.md
@src/stores/task-store.tsx
@src/components/TaskDetail.tsx
@src/components/HorizonScene.tsx
@src/components/TaskNode.tsx
@src/components/TaskCard.tsx
@src/components/TaskSprite.tsx
@src/app/api/tasks/route.ts
@src/app/api/tasks/[id]/route.ts
@src/lib/scene-constants.ts
@src/types/task.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompletionBurst particle effect + dissolution animations on TaskNode</name>
  <files>src/components/CompletionBurst.tsx, src/components/HorizonScene.tsx, src/stores/task-store.tsx, src/components/TaskNode.tsx, src/components/TaskCard.tsx, src/components/TaskSprite.tsx</files>
  <action>
  **task-store.tsx** -- Add to TaskState:
  - `completingTaskIds: Set<string>` (initial: empty Set) -- tracks tasks currently dissolving
  - `droppingTaskIds: Set<string>` (initial: empty Set) -- tracks tasks currently shrinking away

  Add to TaskActions:
  - `startCompletion: (id: string) => void` -- adds id to completingTaskIds set
  - `finishCompletion: (id: string) => void` -- removes id from completingTaskIds and calls removeTask
  - `startDrop: (id: string) => void` -- adds id to droppingTaskIds set
  - `finishDrop: (id: string) => void` -- removes id from droppingTaskIds and calls removeTask

  Export `useIsCompleting(taskId: string)` and `useIsDropping(taskId: string)` selectors.

  **CompletionBurst.tsx** -- New file. R3F component rendered INSIDE Canvas.
  - Props: `{ position: [number, number, number]; onComplete: () => void }`
  - Uses `useRef<THREE.Points>` and `useMemo` to create BufferGeometry with ~24 particles
  - Particle init: all start at origin (0,0,0). Random spherical velocities (speed 1.5-3.5). Colors: white-to-gold gradient (R=1, G=0.85-1.0, B=0.4-1.0 random per particle).
  - `useFrame` animation loop:
    - Track elapsed time via `useRef(performance.now())`
    - Duration: 0.8 seconds
    - Each frame: advance positions by velocity * delta, update `posAttr.needsUpdate = true`
    - Fade opacity: `mat.opacity = 1 - (elapsed / duration)`
    - Call `invalidate()` every frame during animation (demand frameloop)
    - When elapsed > duration: call `onComplete()` to self-remove
  - Render: `<points ref={pointsRef} position={position}>` with `<bufferGeometry>` and `<pointsMaterial size={0.15} vertexColors transparent toneMapped={false} depthWrite={false} sizeAttenuation />`
  - See 06-RESEARCH.md Pattern 3 for full example code

  **HorizonScene.tsx** -- Add completion burst management:
  - Add state: `const [bursts, setBursts] = useState<Array<{ id: string; position: [number, number, number] }>>([])` in the `SceneContents` or `TaskNodes` component (wherever it's inside Canvas for useFrame access).
  - Actually, since CompletionBurst needs to render inside Canvas but burst triggering happens from DOM (TaskDetail), use Zustand to bridge:
    - Add to task-store: `pendingBursts: Array<{ id: string; position: [number, number, number] }>` + `addBurst(id, position)` + `removeBurst(id)` actions
    - In `TaskNodes` (inside Canvas): subscribe to `pendingBursts`, render `<CompletionBurst>` for each, call `removeBurst` on `onComplete`
  - Import CompletionBurst in HorizonScene.tsx

  Actually, simpler approach: manage bursts state locally in TaskNodes since it's inside Canvas:
  - In `TaskNodes` component in HorizonScene.tsx, add local state for active bursts
  - Subscribe to `completingTaskIds` changes from store. When a new ID appears in completingTaskIds, look up that task's current position from positionMap, and add a burst entry
  - Render `{bursts.map(b => <CompletionBurst key={b.id} position={b.position} onComplete={() => { removeBurstFromLocalState(b.id); store.getState().finishCompletion(b.id); }} />)}`

  **TaskCard.tsx** -- Add dissolution animation when isCompleting:
  - Import `useIsCompleting` and `useIsDropping` from task-store
  - When `isCompleting`: fade opacity to 0 and scale to 0.5 over ~0.5s via inline style transition
  - When `isDropping`: scale to 0 quickly (~0.2s) with no fade ceremony -- just shrinks to nothing

  **TaskSprite.tsx** -- Add dissolution animation when isCompleting:
  - Import `useIsCompleting` and `useIsDropping` from task-store
  - When `isCompleting`: animate opacity to 0 and scale to 0 over ~0.5s via useFrame (similar to entrance animation but in reverse). Call `invalidate()` each frame.
  - When `isDropping`: animate scale to 0 rapidly (~0.2s) via useFrame. No opacity fade. Abrupt shrink.

  **TaskNode.tsx** -- Pass through completing/dropping state to children (or let children read from store directly via hooks, which is the cleaner approach -- no changes needed in TaskNode.tsx if TaskCard/TaskSprite use the hooks directly).
  </action>
  <verify>
  - `npm run build` compiles without errors
  - In dev server, trigger a completion (console.log from TaskDetail is replaced in Task 2 with actual handler -- for now, manually call `store.getState().startCompletion(taskId)` from browser console to test the animation)
  - Particle burst appears at the task's position with white/gold colors
  - Particle burst fades out over ~0.8s then self-removes
  - Task card/sprite dissolves (fades + shrinks) during completion
  - Drop animation: task shrinks to zero rapidly, no particles
  </verify>
  <done>CompletionBurst renders a white/gold particle burst at task position. Task cards and sprites dissolve (fade+shrink) on completion and shrink-to-zero on drop. Animations self-clean-up after completion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Complete and Drop buttons in TaskDetail + undo toast + server persistence + GET filter</name>
  <files>src/components/TaskDetail.tsx, src/app/api/tasks/route.ts</files>
  <action>
  **TaskDetail.tsx** -- Wire the Complete button:
  1. `handleComplete(taskId)`:
     - Snapshot the task: `const snapshot = { ...task }` (deep copy of current task data for undo revert)
     - Call `store.getState().startCompletion(taskId)` to trigger dissolution animation
     - Call `store.getState().clearSelection()` to close the panel (or show brief success state first)
     - Actually, per CONTEXT.md: "After complete or drop: Panel shows a brief success state (~1 second), then auto-closes." So:
       - Set local state `actionResult: 'completed' | 'dropped' | null` to 'completed'
       - Show a brief overlay in the panel content area: checkmark + "Done" text for ~1s
       - After 1s, call `clearSelection()` to close panel
     - Show undo toast (see below)
     - Set a 4-second timeout. When timeout expires, fire `PATCH /api/tasks/${taskId}` with `{ status: 'completed' }`. If PATCH fails, revert: call `store.getState().addTask(snapshot)` to restore the task.
     - Store the timeout ID in a ref so the undo callback can clear it.

  2. Undo toast:
     - Add local state for undo: `undoState: { taskId: string; snapshot: TaskRow; timeoutId: ReturnType<typeof setTimeout> } | null`
     - Render undo toast as a fixed-position element inside TaskDetail (or as a separate div). Style: similar to InputBubble toast -- dark glass pill, positioned at bottom-center or bottom-left. Text: "Task completed" + "Undo" button.
     - The toast is visible for 4 seconds. Clicking "Undo" calls:
       - `clearTimeout(undoState.timeoutId)` -- cancel server persist
       - `store.getState().finishCompletion(taskId)` is wrong here because that removes the task. Instead, need a `cancelCompletion(id)` action that removes from completingTaskIds WITHOUT calling removeTask, and restores the snapshot via addTask
       - Actually simpler: have undo do: clear the completing state (remove from completingTaskIds), re-add the snapshot task to the store. Add a `cancelCompletion: (id: string) => void` action to store that just removes from completingTaskIds set.
       - `store.getState().cancelCompletion(taskId)` + `store.getState().addTask(snapshot)` -- but addTask adds to newTaskIds which triggers entrance animation. Instead, just do: remove from completingTaskIds + add task back to tasks array without newTaskIds. Add action: `restoreTask: (task: TaskRow) => void` that adds to tasks but NOT to newTaskIds.
     - After 4s with no undo, the toast fades out.
     - Toast styling: inline styles, background `rgba(18, 18, 26, 0.9)`, backdropFilter blur, border, borderRadius 16, padding '10px 16px', position fixed, bottom 80 (above InputBubble), left '50%', transform translateX(-50%), zIndex 130, transition opacity 0.5s for fade.

  3. `handleDrop(taskId)`:
     - Call `store.getState().startDrop(taskId)` to trigger shrink animation
     - Set `actionResult` to 'dropped' for brief panel state
     - After ~0.3s (shrink animation time), call `finishDrop(taskId)` to remove from store
     - After 1s, call `clearSelection()` to close panel
     - Fire `PATCH /api/tasks/${taskId}` with `{ status: 'dropped' }` immediately (no undo window)
     - No undo toast for drop -- per CONTEXT.md, drop is intentionally no-ceremony

  **task-store.tsx** -- Additional actions needed (from above):
  - `cancelCompletion: (id: string) => void` -- removes from completingTaskIds only
  - `restoreTask: (task: TaskRow) => void` -- adds task back to tasks array without adding to newTaskIds

  **GET /api/tasks route.ts** -- Filter to active tasks only:
  - Import `eq` from 'drizzle-orm'
  - Change: `const allTasks = await db.select().from(tasks)` to `const allTasks = await db.select().from(tasks).where(eq(tasks.status, 'active'))`
  - This ensures completed and dropped tasks don't reappear on page refresh
  - Import the eq operator (it's already used in [id]/route.ts so it's available)

  Wire the buttons:
  - Replace `console.log('complete', task.id)` stub with `handleComplete(task.id)`
  - Replace `console.log('drop', task.id)` stub with `handleDrop(task.id)`
  - Add hover states to Complete and Drop buttons for polish
  </action>
  <verify>
  - `npm run build` compiles without errors
  - Click a task -> click Complete -> panel shows brief "Done" state -> particle burst plays -> task dissolves from scene -> undo toast appears -> after 4s toast fades and task is gone from GET /api/tasks response
  - Click Undo within 4s -> task reappears in scene, no PATCH was sent
  - Click a task -> click Drop -> task shrinks to nothing immediately, no particles, panel closes, task gone from scene
  - Refresh page -> completed/dropped tasks do not reappear (GET filters by status=active)
  - `curl http://localhost:3000/api/tasks` returns only active tasks
  </verify>
  <done>Complete action triggers dissolution + particle burst + undo toast + PATCH to 'completed'. Drop action triggers instant shrink + PATCH to 'dropped'. GET /api/tasks returns only active tasks. Undo within 4s restores the task.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero type errors
- Complete flow: click task -> Complete -> dissolution animation with white/gold particle burst -> undo toast -> task removed from scene and Postgres status updated to 'completed'
- Undo flow: click Complete -> click Undo within 4s -> task reappears, no server update
- Drop flow: click task -> Drop -> instant shrink, no particles, no undo -> task removed, Postgres status 'dropped'
- Page refresh: completed/dropped tasks do not reappear
- Emotional contrast: completion feels celebratory (particles + glow), drop feels clinical (abrupt shrink)
</verification>

<success_criteria>
1. CompletionBurst renders ~24 white/gold particles that burst outward and fade over 0.8s
2. Task dissolution animation plays simultaneously with particle burst on complete
3. Undo toast visible for 4 seconds; clicking undo restores the task completely
4. Drop is visually distinct from complete -- instant shrink, no particles, no undo
5. GET /api/tasks filtered by status='active' -- completed/dropped tasks survive in DB but don't render
6. Panel shows brief success state before auto-closing after complete/drop
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-interactions/06-02-SUMMARY.md`
</output>
