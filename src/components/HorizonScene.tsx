'use client';

/** Stub placeholder for the 3D scene — replaced in Phase 3. */
export default function HorizonScene() {
  return (
    <div className="flex h-full w-full items-center justify-center text-text-secondary text-sm">
      Scene placeholder — Phase 3
    </div>
  );
}
